{
  "name": "live-counting",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "migrate": "npm --prefix apps/web run migrate",
    "seed": "npm --prefix apps/web run seed"
  }
}
